<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ApprovalType">
    <!-- 선택할 수 있는 결재 상태 목록 조회 -->
    <select id="getList" resultType="com.kedu.ggirick_client_backend.dto.approval.ApprovalTypeDTO">
        SELECT * FROM approval_type;
    </select>
</mapper>