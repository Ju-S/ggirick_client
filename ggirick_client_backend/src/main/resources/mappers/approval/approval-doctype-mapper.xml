<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ApprovalDocType">
    <!-- 결재문서 양식 종류 목록 조회 -->
    <select id="getList" resultType="com.kedu.ggirick_client_backend.dto.approval.ApprovalDocTypeDTO">
        SELECT *
        FROM approval_doc_type
    </select>
</mapper>