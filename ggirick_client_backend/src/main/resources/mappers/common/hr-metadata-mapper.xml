<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="hr-metadata">
    <!-- 부서 목록 조회 ( 메타 데이터 조회용 ) -->
    <select id="getAllDepartments" resultType="com.kedu.ggirick_client_backend.dto.common.DepartmentDTO">
        SELECT * FROM department
    </select>

    <!-- 직급 목록 조회 ( 메타 데이터 조회용 ) -->
    <select id="getAllJobs" resultType="com.kedu.ggirick_client_backend.dto.common.JobDTO">
        SELECT * FROM job
    </select>

    <!-- 조직 목록 조회 ( 메타 데이터 조회용 ) -->
    <select id="getAllOrganizations" resultType="com.kedu.ggirick_client_backend.dto.common.OrganizationDTO">
        SELECT * FROM organization
    </select>

    <!-- 권한 목록 조회 ( 메타 데이터 조회용 ) -->
    <select id="getAllAuthorities" resultType="com.kedu.ggirick_client_backend.dto.common.AuthorityDTO">
        SELECT * FROM authority
    </select>
</mapper>