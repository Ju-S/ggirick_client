<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="BoardGroup">
    <select id="getGroupList" resultType="com.kedu.ggirick_client_backend.dto.board.BoardGroupDTO">
        SELECT * FROM board_group bg
        JOIN board_group_employee bge ON bg.id=bge.board_group_id
        WHERE bge.employee_id=#{id}
        ORDER BY bg.id
    </select>
</mapper>